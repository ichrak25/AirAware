# ==================== AirAware IAM Configuration ====================
jnosql.document.database=AirAwareIAM
jnosql.mongodb.host=localhost:27017
# JWT Configuration
jwt.issuer=urn:airaware:iam
jwt.lifetime.duration=3600
key.pair.lifetime.duration=86400
key.pair.cache.size=5

# Argon2 Password Hashing
argon2.saltLength=16
argon2.hashLength=32
argon2.iterations=3
argon2.memory=65536
argon2.threadNumber=4

# SMTP Email Configuration
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=aouadniichrak@gmail.com
smtp.password=kzrq rpbz gwrt bsrf
smtp.starttls.enable=true

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:8080
cors.allowed.methods=GET, POST, PUT, DELETE, OPTIONS
cors.allowed.headers=Content-Type, Authorization, X-Requested-With

# Custom Roles (optional - extends default roles)
airaware.roles=environmental_officer,city_manager,technician